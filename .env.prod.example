# Production Environment Configuration for Claude Connector

# Port for the MCP server to listen on
MCP_SERVER_PORT=8080

# REQUIRED: A strong, randomly generated secret key for authentication
# Generate with: openssl rand -base64 32
API_SECRET_KEY=CHANGE_THIS_TO_A_VERY_LONG_SECURE_RANDOM_STRING

# The path to the CSV file *inside the container*
CSV_FILE_PATH=/data/medical_data.csv

# REQUIRED: Your domain name for the Claude connector
# Example: claude-connector.yourdomain.com
CLAUDE_DOMAIN=claude-connector.yourdomain.com

# Traefik TLS certificate resolver name
# This should match your Traefik configuration
TLS_RESOLVER=myresolver

# Optional: Network name for Traefik integration
# DOCKER_NETWORK=claude-network